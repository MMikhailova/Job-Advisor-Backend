'use strict';

/**
 * diversity-inclusion service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::diversity-inclusion.diversity-inclusion');
