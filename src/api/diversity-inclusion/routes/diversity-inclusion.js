'use strict';

/**
 * diversity-inclusion router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::diversity-inclusion.diversity-inclusion');
