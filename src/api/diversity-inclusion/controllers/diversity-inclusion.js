'use strict';

/**
 * diversity-inclusion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::diversity-inclusion.diversity-inclusion');
